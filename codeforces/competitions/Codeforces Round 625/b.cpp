#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n;
    cin>>n;

    int b[n];
    for(int i=0;i<n;++i) cin>>b[i];

    ll sum = 0;

    ll dp[n];

    memset(dp, 0, sizeof(dp));

    
}
